export function Footer() {
  return (
    <footer className="w-full px-6 py-4 text-center text-sm text-gray-500 border-t">
      © {new Date().getFullYear()} EcoTrack — Track your impact, one habit at a time.
    </footer>
  );
}